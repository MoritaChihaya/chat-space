.Side-bar
  .left-header
    .box
      %p.user-name 
        = current_user.name
      %ul.menu
        %li.new-group
          = link_to "#", class:"icon" do
            = icon('far', 'edit')
        %li.edit-user
          = link_to edit_user_path(current_user), class:"icon" do
            = icon('fas', 'cog')
  .groups
    .group
      %p.name techcamp,1
      %p.latest-massage まだメッセージがありません,1
    .group
      %p.name techcamp,1
      %p.latest-massage まだメッセージがありません,1